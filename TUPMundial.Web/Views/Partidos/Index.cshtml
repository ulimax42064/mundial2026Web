@model List<TUPMundial.Web.Models.Partido>
@{ ViewData["Title"] = "Partidos"; Layout = "~/Views/Shared/_Layout.cshtml"; }

<div class="header">
    <div class="header-row">
        <div class="header-left">
            <span style="font-size:2rem;">⚽</span>
            <div>
                <div class="header-title">Mundial 2026</div>
                <div class="header-sub">Hola, @ViewBag.Nombre 👋</div>
            </div>
        </div>
        <div style="display:flex; flex-direction:column; align-items:flex-end; gap:8px;">
            <a href="@Url.Action("Logout", "Auth")"><button class="btn-salir">Salir</button></a>
            <a href="@Url.Action("Index", "Tickets")" class="mis-tickets-link">🎟 Mis tickets</a>
        </div>
    </div>
</div>
<div class="header-strip"></div>

<div class="filtros">
    <a href="@Url.Action("Index", "Partidos")">
        <button class="filtro-btn @(ViewBag.GrupoSel == "Todos" ? "activo" : "")">Todos</button>
    </a>
    @foreach (var g in (ViewBag.Grupos as List<string>) ?? new List<string>())
    {
        <a href="@Url.Action("Index", "Partidos", new { grupo = g })">
            <button class="filtro-btn @(ViewBag.GrupoSel == g ? "activo" : "")">@g</button>
        </a>
    }
</div>

<div class="container" style="padding-top:8px;">
    @foreach (var p in Model)
    {
        <a href="@Url.Action("Detalle", "Partidos", new { id = p.Id })">
            <div class="partido-card">
                <div>
                    <div class="partido-grupo">@p.Grupo</div>
                    <div class="partido-equipos">@p.Flags1 @p.Equipo1 vs @p.Equipo2 @p.Flags2</div>
                    <div class="partido-info">
                        📅 @p.Fecha<br />
                        🏟 @p.Estadio
                    </div>
                </div>
                <div class="partido-arrow">›</div>
            </div>
        </a>
    }
</div>
